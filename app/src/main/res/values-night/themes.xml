<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode. -->
    <style name="Theme.Fitsoul" parent="android:Theme.Material.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:colorPrimary">@color/fitness_green_primary</item>
        <item name="android:colorPrimaryDark">@color/dark_green</item>
        <item name="android:colorAccent">@color/fitness_green_light</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>
